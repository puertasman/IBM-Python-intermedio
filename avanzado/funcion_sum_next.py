""" FUNCIÓN SUM """

numeros = [1, 2, 3, 4, 5]

suma = sum(numeros)
print(f"Resultado de la suma {suma}")

resultado = sum(numeros, 20)
print(f"Resultado de la suma con 20 iniciales {resultado}")

# Funcion next

iterador = iter(numeros)

# Next obtiene el siguiente elemento
print(f"Siguiente elemento del iterable {next(iterador)}")
print(f"Siguiente elemento del iterable {next(iterador)}")
print(f"Siguiente elemento del iterable {next(iterador)}")
print(f"Siguiente elemento del iterable {next(iterador)}")
print(f"Siguiente elemento del iterable {next(iterador)}")
